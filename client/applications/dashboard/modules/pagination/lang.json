{
  "en": {
    "default_pagination": "Default Pagination",
    "pagination_des": "It only needs to configure limit in config.table, If the total of the list is larger than limit, it will automatically generate pagination."
  },
  "zh-CN": {
    "default_pagination": "默认分页",
    "pagination_des": "只需要在config.table中配置limit, 如果列表的total大于limit，会自动生成分页。"
  }
}